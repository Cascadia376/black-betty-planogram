import { ExternalLink, Lightbulb, MapPin } from "lucide-react";
import { useMemo, useState } from "react";
import { Link, useParams } from "react-router-dom";
import { Badge, Button, Card, DataState, EmptyState, Field, PageHeader, formatDate, humanize, inputClass } from "../../components/ui";
import { campaignDisplayAreaCompatibility } from "../../domain/campaignDisplayAllocation";
import { usePlatform } from "../../services/PlatformProvider";
import { CampaignWorkflowStepper, campaignStoreReadiness } from "./campaignWorkflow";

export function CampaignStoreAllocationPage() {
  const { campaignId } = useParams(); const platform = usePlatform(); const { data, loading, error } = platform; const campaign = data?.campaigns.find((item) => item.id === campaignId);
  const [view, setView] = useState<"display" | "store">("display"); const [filter, setFilter] = useState("all"); const [message, setMessage] = useState<string>();
  const included = useMemo(() => data?.campaignStores.filter((item) => item.campaignId === campaign?.id && item.included) ?? [], [campaign?.id, data?.campaignStores]);
  const readiness = campaignStoreReadiness(campaign, data);
  const act = async (work: () => Promise<unknown>) => { try { setMessage(undefined); await work(); } catch (cause) { setMessage(cause instanceof Error ? cause.message : "Unable to update store allocation."); } };
  return <DataState loading={loading} error={error}>{!campaign || !data ? <EmptyState title="Campaign not found" message="Unable to open store allocation." /> : <div className="space-y-5"><PageHeader eyebrow="Stores" title={campaign.name} description={`${formatDate(campaign.startDate)} to ${formatDate(campaign.endDate)} · Where should each campaign display go in each participating store?`} actions={readiness.complete ? <Button>Continue to Review</Button> : undefined} /><CampaignWorkflowStepper campaign={campaign} data={data} current="stores" />
    <Card><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-semibold">Store allocation readiness</h2><p className="mt-1 text-sm text-text-secondary">{readiness.included} included · {readiness.ready} ready · {readiness.needsReview} need review · {readiness.notStarted} not started</p><p className="text-sm text-text-muted">{readiness.completePlacements} / {readiness.totalPlacements} placements complete</p></div><Badge tone={readiness.complete ? "success" : "warning"}>{readiness.complete ? "Ready for review" : `${readiness.needsReview} exceptions`}</Badge></div></Card>{message && <div role="alert" className="rounded-md border border-error/30 bg-error-subtle p-3 text-sm text-error">{message}</div>}
    <Card><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-semibold">Participating stores</h2><p className="text-sm text-text-secondary">Include stores before physical placement decisions are made.</p></div><Button variant="secondary" onClick={() => act(() => platform.setCampaignStores({ campaignId: campaign.id, storeIds: data.stores.map((store) => store.id) }))}>Include all stores</Button></div><div className="mt-3 grid gap-2 md:grid-cols-2">{data.stores.map((store) => { const checked = included.some((item) => item.storeId === store.id); return <label key={store.id} className="flex gap-2 rounded border border-border p-3 text-sm"><input type="checkbox" checked={checked} onChange={() => act(() => platform.setCampaignStores({ campaignId: campaign.id, storeIds: checked ? included.filter((item) => item.storeId !== store.id).map((item) => item.storeId) : [...included.map((item) => item.storeId), store.id] }))} /><span><b>{store.name}</b><span className="block text-xs text-text-muted">{store.code}</span></span></label>; })}</div></Card>
    <div className="flex flex-wrap gap-2"><Button variant={view === "display" ? "primary" : "secondary"} onClick={() => setView("display")}>By display</Button><Button variant={view === "store" ? "primary" : "secondary"} onClick={() => setView("store")}>By store</Button><select aria-label="Allocation filter" className={inputClass} value={filter} onChange={(event) => setFilter(event.target.value)}><option value="all">All allocations</option><option value="attention">Needs attention</option><option value="ready">Ready</option></select></div>
    {view === "display" ? <div className="space-y-4">{data.campaignDisplays.filter((item) => item.campaignId === campaign.id).map((display) => <DisplayAllocations key={display.id} displayId={display.id} campaignId={campaign.id} filter={filter} />)}</div> : <div className="space-y-4">{included.map((scope) => <StoreAllocations key={scope.id} storeId={scope.storeId} campaignId={campaign.id} filter={filter} />)}</div>}
  </div>}</DataState>;
}

function DisplayAllocations({ displayId, campaignId, filter }: { displayId: string; campaignId: string; filter: string }) { const platform = usePlatform(); const { data } = platform; if (!data) return null; const display = data.campaignDisplays.find((item) => item.id === displayId)!; const scopes = data.campaignStores.filter((item) => item.campaignId === campaignId && item.included); const rows = scopes.map((scope) => data.campaignDisplayAssignments.find((item) => item.campaignDisplayId === displayId && item.storeId === scope.storeId)).filter((item) => showAllocation(item?.status, filter)); return <Card><div className="flex flex-wrap items-center justify-between gap-2"><div><h2 className="font-semibold">{display.name}</h2><p className="text-sm text-text-secondary">{display.placementMode === "STANDARD" ? "Standard — suggestions require buyer acceptance." : "Store-specific — choose an explicit location for every store."}</p></div>{display.placementMode === "STANDARD" && <Button onClick={() => void platform.suggestCampaignDisplay({ campaignId, campaignDisplayId: displayId })}><Lightbulb className="h-4 w-4" />Suggest for all stores</Button>}</div><div className="mt-3 space-y-2">{rows.length ? rows.map((assignment) => assignment ? <AllocationRow key={assignment.id} assignmentId={assignment.id} /> : null) : scopes.filter((scope) => !data.campaignDisplayAssignments.some((item) => item.campaignDisplayId === displayId && item.storeId === scope.storeId)).map((scope) => <UnplannedRow key={scope.id} displayId={displayId} campaignId={campaignId} storeId={scope.storeId} />)}</div></Card>; }
function StoreAllocations({ storeId, campaignId, filter }: { storeId: string; campaignId: string; filter: string }) { const { data } = usePlatform(); if (!data) return null; const store = data.stores.find((item) => item.id === storeId)!; const displays = data.campaignDisplays.filter((item) => item.campaignId === campaignId); return <Card><h2 className="font-semibold">{store.name}</h2><div className="mt-3 space-y-2">{displays.map((display) => { const allocation = data.campaignDisplayAssignments.find((item) => item.campaignDisplayId === display.id && item.storeId === storeId); return allocation && showAllocation(allocation.status, filter) ? <AllocationRow key={allocation.id} assignmentId={allocation.id} /> : !allocation && filter !== "ready" ? <UnplannedRow key={display.id} displayId={display.id} campaignId={campaignId} storeId={storeId} /> : null; })}</div></Card>; }
function UnplannedRow({ displayId, campaignId, storeId }: { displayId: string; campaignId: string; storeId: string }) { const { data, suggestCampaignDisplay } = usePlatform(); if (!data) return null; const display = data.campaignDisplays.find((item) => item.id === displayId)!; const store = data.stores.find((item) => item.id === storeId)!; return <div className="flex flex-wrap items-center justify-between gap-2 rounded border border-warning/30 p-3 text-sm"><span><b>{store.name}</b> · {display.name}<span className="block text-text-warning">No placement decision</span></span><Button variant="secondary" onClick={() => void suggestCampaignDisplay({ campaignId, campaignDisplayId: displayId, storeIds: [storeId] })}>{display.placementMode === "STANDARD" ? "Suggest" : "Start placement"}</Button></div>; }
function AllocationRow({ assignmentId }: { assignmentId: string }) { const platform = usePlatform(); const { data } = platform; const [expanded, setExpanded] = useState(false); if (!data) return null; const assignment = data.campaignDisplayAssignments.find((item) => item.id === assignmentId)!; const display = data.campaignDisplays.find((item) => item.id === assignment.campaignDisplayId)!; const store = data.stores.find((item) => item.id === assignment.storeId)!; const candidates = data.displayAreas.filter((item) => item.storeId === store.id && item.active); const area = data.displayAreas.find((item) => item.id === assignment.displayAreaId); return <div className="rounded border border-border p-3 text-sm"><div className="flex flex-wrap items-center justify-between gap-2"><div><b>{store.name}</b> · {display.name}<span className="block text-text-muted">{area ? `${area.name} (${area.displayNumber})${area.active ? "" : " · inactive historical area"}` : assignment.suggestionDisplayAreaId ? `Suggested: ${data.displayAreas.find((item) => item.id === assignment.suggestionDisplayAreaId)?.name}` : "Choose physical location"}</span>{assignment.suggestionReasons?.map((reason) => <span key={reason} className="block text-xs text-text-muted">• {reason}</span>)}</div><div className="flex flex-wrap gap-2"><Badge tone={assignment.status === "ASSIGNED" || assignment.status === "EXCLUDED" ? "success" : "warning"}>{humanize(assignment.status)}</Badge>{assignment.status === "SUGGESTED" && <Button onClick={() => void platform.updateCampaignDisplayAssignment({ campaignDisplayAssignmentId: assignment.id, displayAreaId: assignment.suggestionDisplayAreaId, status: "ASSIGNED" })}>Accept suggestion</Button>}<Button variant="secondary" onClick={() => setExpanded(!expanded)}>{expanded ? "Close" : "Choose / quantities"}</Button><Link className="inline-flex min-h-9 items-center gap-1 rounded-md border border-border px-3 font-semibold" to={`/stores/${store.id}/floorplan${area ? `?area=${area.id}` : ""}`}><MapPin className="h-4 w-4" />Floorplan</Link></div></div>{expanded && <div className="mt-3 grid gap-3 border-t border-border pt-3"><Field label="Physical display area"><select className={inputClass} value={area?.active ? assignment.displayAreaId ?? "" : ""} onChange={(event) => { if (event.target.value) void platform.updateCampaignDisplayAssignment({ campaignDisplayAssignmentId: assignment.id, displayAreaId: event.target.value, status: "ASSIGNED", placementSource: "BUYER_SELECTED" }); }}><option value="">Choose active verified location…</option>{candidates.map((candidate) => { const result = campaignDisplayAreaCompatibility(display, candidate, data); return <option key={candidate.id} value={candidate.id} disabled={result.status === "incompatible"}>{candidate.name} · {candidate.localCode ?? candidate.displayNumber} · {humanize(result.status)}</option>; })}</select></Field><div className="flex gap-2"><Button variant="secondary" onClick={() => void platform.updateCampaignDisplayAssignment({ campaignDisplayAssignmentId: assignment.id, status: "EXCLUDED" })}>Mark not used in this store</Button>{area && <a className="inline-flex min-h-9 items-center gap-1 rounded-md border border-border px-3 font-semibold" href={`/stores/${store.id}/floorplan?area=${area.id}`}><ExternalLink className="h-4 w-4" />View on floorplan</a>}</div><QuantityEditor assignmentId={assignment.id} displayId={display.id} /></div>}</div>; }
function QuantityEditor({ assignmentId, displayId }: { assignmentId: string; displayId: string }) { const platform = usePlatform(); const { data } = platform; if (!data) return null; const products = data.campaignDisplayAssignmentProducts.filter((item) => item.campaignDisplayAssignmentId === assignmentId); return <div><h3 className="font-semibold">Store quantities</h3><div className="mt-2 space-y-2">{products.map((item) => { const product = data.products.find((candidate) => candidate.id === item.productId); const member = data.campaignDisplayProducts.find((candidate) => candidate.id === item.campaignDisplayProductId); return <div className="grid items-center gap-2 rounded border border-border p-2 md:grid-cols-[1fr_auto_auto]"><span><b>{product?.sku}</b> · {product?.name}<span className="block text-xs text-text-muted">{member?.role} · case pack {product?.casePack ?? "—"} · recommended {item.recommendedCases ?? "No recommendation"}</span></span><input aria-label={`Quantity for ${product?.sku}`} type="number" min="0" className={inputClass} defaultValue={item.caseQuantity ?? ""} onBlur={(event) => { if (event.target.value !== "") void platform.updateCampaignDisplayAssignmentProduct({ campaignDisplayAssignmentProductId: item.id, caseQuantity: Number(event.target.value) }); }} /><Badge tone={item.buyerOverride ? "warning" : "neutral"}>{item.buyerOverride ? "Buyer override" : "Recommended"}</Badge></div>; })}</div>{products.length > 0 && <Button className="mt-2" variant="secondary" onClick={() => { const value = window.prompt("Apply cases to all stores for this display product:"); if (value !== null) void platform.applyCampaignDisplayQuantity({ campaignDisplayId: displayId, campaignDisplayProductId: products[0].campaignDisplayProductId, caseQuantity: Number(value) }); }}>Apply first product quantity to all stores</Button>}</div>; }
function showAllocation(status: string | undefined, filter: string) { return filter === "all" || (filter === "ready" && (status === "ASSIGNED" || status === "EXCLUDED")) || (filter === "attention" && status !== "ASSIGNED" && status !== "EXCLUDED"); }
